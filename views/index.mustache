<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Robots - Home</title>
    <link rel="stylesheet" href="/static/main.css">
  </head>
  <body>
    <div class="heading">
      <h1>User Directory</h1>
      <a href="/available"><h1>See Available</h1></a>
    </div>
    <main class="container">
      {{#results}}
      <div class="each-user">
        <div class="avatar-img">
          <a class="click-me" href="directory/{{id}}"><img class="images" src="{{avatar}}" alt="avatar"></a>
        </div>
        <div class="avatar-name">
          <h3>{{name}}</h3>
        </div>
        <div class="current-pos">
          <p>{{job}}{{^job}}{{/job}}</p>
          <p>{{company}}{{^company}}<p class="avail">Available for hire</p>{{/company}}</p>
        </div>
      </div>
      {{/results}}
    </main>

  </body>
</html>
